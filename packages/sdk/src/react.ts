/**
 * XKOVA React entry point.
 *
 * Layer: React provider + hooks (re-exports @xkova/sdk-react).
 * Audience: browser-based React apps that can mount providers.
 * Not included: UI components or browser-only helpers; requires React context.
 */
export * from "@xkova/sdk-react";

