/**
 * XKOVA browser entry point.
 *
 * Layer: browser-only IEE (SafeApprove) helpers (re-exports @xkova/sdk-browser).
 * Audience: browser apps that need to launch the IEE (SafeApprove) flow.
 * Not included: Node/SSR support or React hooks.
 */
export * from "@xkova/sdk-browser";
